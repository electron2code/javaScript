const username = "false";

if (username) {
    console.log("username is found", username);
}

// falsy values:
// "", 0, 0n, -0, false, null, undefined, NaN

// truthy values:
// " ", 1, -1, [], {}, "false",